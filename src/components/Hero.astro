<hero class="scrollable">
    <div class="img">
        <picture>
            <source srcset="/assets/phone.webp" type="image/webp">
            <img src="/assets/phone.png" alt="" importance="low">
        </picture>
    </div>
    <div class="img">
        <picture>
            <source srcset="/assets/desktop.webp" type="image/webp">
            <img rel="preload" loading="lazy" class="desktop" src="/assets/desktop.png" alt="Rund theme">
        </picture>
    </div>
</hero>
<div class="scrollbar--wrapper">
    <div class="scrollbar">
        <div class="scrollbar-thumb"></div>
    </div>
</div>
<style>
    .scrollbar {
        width: 100%;
        height: 3px;
        background-color: #ddd;
    }
    .scrollbar-thumb {
        background-color: #888;
        width: var(--thumb-width);
        height: inherit;
        transform: translateX(var(--thumb-x-pos));
    }
</style>
<script>
    window.addEventListener("load", function() {
        const hero = document.querySelector(".scrollable");
        //const scrollbarThumb = document.querySelector(".scrollbar-thumb");

        hero.nextElementSibling.style.setProperty('--thumb-width', window.innerWidth / hero.scrollWidth * 100 +"%");
        console.log(window.innerWidth / hero.scrollWidth * 100 +"%")

        hero.onscroll = function() {
            hero.nextElementSibling.style.setProperty('--thumb-x-pos', hero.scrollLeft / window.innerWidth * 100 +"%");
        };
    });
</script>

<section>
    <h2>Theme – Rund</h2>
    <p>A Shopify theme for sites selling visually-driven products such as apparel, beauty products, home decor, and furniture.</p>
</section> 
<style>
    hero {
        position: relative;
        max-width: 100%;
        display: flex;
        align-items: center;
        flex-wrap: nowrap;
        flex-shrink: 0;
        overflow: hidden;
        overflow-x: auto;
        scroll-behavior: smooth;
        scroll-snap-type: x mandatory;
        -ms-overflow-style: none;  /* IE and Edge */
        scrollbar-width: none;  /* Firefox */
    }
    hero::-webkit-scrollbar {
        display: none;
    }
    p {
        margin-top: 0;
        font-size: 2rem;
        max-width: 60ch;
    }
    hero .img {
        min-width: 100%;
    }
    hero .img + .img {
        padding-left: 4rem;
    }
    hero .img:nth-child(1) {
        min-width: 25%;
    }
    hero .img:nth-child(2) {
        min-width: 105%;
        max-width: 120rem;
    }
    hero .img img {
        width: 100%;
    }
    hero .img:last-child::after {
        display: block;
        content: '';
    }
    @media(min-width: 1600px) {
        hero {
            padding-left: calc(50vw - var(--page-width) / 2 + 4rem);
            padding-right: calc(50vw - var(--page-width) / 2 + 4rem);
            scroll-padding-left: calc(50vw - var(--page-width) / 2 + 4rem);
        }
        
        hero .img:last-child::after {
            width: calc(50vw - var(--page-width) / 2 + 4rem);s
        }
    }
</style>